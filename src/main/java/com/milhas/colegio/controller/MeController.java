package com.milhas.colegio.controller;

/**
 * (Disabled) Temporary debug controller that was added during troubleshooting.
 * Keeping the file without Spring annotations so it does not affect the app.
 */
public class MeController {
    // intentionally empty
}

